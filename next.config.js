/** @type {import('next').NextConfig} */

const nextConfig = {
  output: "export",
  assetPrefix: "",
  basePath: "",
  images: { unoptimized: true },
  trailingSlash: true,
};

export default nextConfig;
